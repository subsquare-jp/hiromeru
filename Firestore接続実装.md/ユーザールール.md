目的:
ヒロメルに以下3ページを追加し、/post からユーザーがルールを確認できるようにしたい。

- /kiyaku（利用条件）
- /image_rule（画像投稿ルール）
- /18over（18禁注意）

最重要条件:
今回の目的は「静的ページ追加とリンク設置のみ」です。
既存の Auth / キーパス / 投稿機能 / Storage / HomeButton を絶対に壊さないこと。

絶対に守ること:
- AuthProvider を触らない
- ログイン機能を触らない
- キーパスロジックを触らない
- Storage処理を触らない
- layout.tsx を不要に変更しない
- UI大改修をしない
- 既存の投稿処理を変更しない

---

■ 実装内容

① 3ページ追加（Next.js App Router）

以下のパスでページを作成する:

- app/kiyaku/page.tsx
- app/image_rule/page.tsx
- app/18over/page.tsx

内容はそれぞれ以下のmdファイルの内容をそのまま表示する形でOK:

- kiyaku.md
- image_rule.md
- 18over.md

実装方法はシンプルでよい:
- ページ内にテキストをそのまま表示
- 軽い余白だけ付ける（max-widthなど）
- スタイリングは最小限

---

② /post ページにリンク追加

投稿フォームの上部または下部に、以下の1行を追加:

投稿前に以下をご確認ください：
利用条件 / 画像投稿ルール / 18禁注意

それぞれリンク:
- /kiyaku
- /image_rule
- /18over

デザイン要件:
- 小さめテキストでOK
- 目立たせすぎない
- 既存UIを壊さない

---

③ build確認
- エラーなく build 通ること
- 既存機能が壊れていないこと

---

■ 完了条件
- /kiyaku が表示される
- /image_rule が表示される
- /18over が表示される
- /post にリンクが表示される
- クリックで各ページに遷移できる
- ログイン・投稿・キーパス機能が壊れていない

---

■ 最後に報告してほしいこと
- 追加したファイル一覧
- /post にどこへリンクを追加したか
- build結果
- Akiが確認すること3つ