目的:
ヒロメルの規約系ページの扱いを明確にしたい。
Akiの意図は、
- /kiyaku
- /image_rule
- /18over
の3ページを「正本（マスター）」として公開し、
/post ではその3ページへのリンクだけを見せる構成にすることです。

最重要条件:
今回の目的は「3ページを正本として扱う構成の確認・必要なら微修正」のみです。
既存の Auth / キーパス / 投稿 / Storage / HomeButton を絶対に壊さないでください。

Akiの意図:
- 3ページは誰でも読める公開ページであること
- 規約やルールは後から更新される前提でよい
- /post に本文を複製するのではなく、マスターである3ページへリンクすればよい
- ユーザビリティが最優先
- SEOは主目的ではない

やってほしいこと:
1. 現在の実装が以下になっているか確認する
   - /kiyaku が公開ページとして表示される
   - /image_rule が公開ページとして表示される
   - /18over が公開ページとして表示される
   - /post には3ページへのリンクがある
2. /post に各ページ本文を複製している箇所があれば削除し、リンクだけにする
3. 3ページを今後の正本として扱う前提で、/post のリンク文言を必要最小限で整える
4. 既存のログイン・キーパス・投稿・画像アップロード機能を壊していないことを確認する
5. build を通す

/ post での望ましい表示:
投稿前に以下をご確認ください：
利用条件 / 画像投稿ルール / 18禁注意

リンク先:
- /kiyaku
- /image_rule
- /18over

絶対にやらないこと:
- AuthProvider を触る
- AuthButtons を触る
- layout.tsx を不要に触る
- キーパスロジックを変える
- Storage処理を変える
- UI大改修
- 3ページの法務文言を勝手に書き換える

完了条件:
- 3ページが誰でも見られる公開ページとして存在する
- /post から3ページへ飛べる
- /post に本文の重複がない
- 既存機能が壊れていない
- build が通る

最後に報告してほしいこと:
- 確認したファイル
- 修正したファイル（あれば）
- /post はリンクのみになっているか
- build結果
- Akiが確認すること2つ